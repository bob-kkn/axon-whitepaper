# Axon Design Validation Summary

> A record of validating the whitepaper's core claims through ten independent prototypes — five validation slices, one group-channel extension of C, three integration slices, and one end-to-end slice that wires both the issuance and payment paths into a single process. For investor and partner review.
> All results are pre-testnet prototype output; confirmation is left to testnet measurement.

## In brief

The Axon whitepaper makes three large claims — that the **issuance economics** of compute supply are safe against fraud, that the **verification** (re-execution adjudication) protecting that issuance is **actually feasible**, and that the **payment and verification plumbing** built on top **withstands attack**. Within the whitepaper these three axes are arguments, but we did not leave them as assertions. Five independent prototypes each realized a claim in code and stress-tested it under adversarial conditions, and all three axes passed a first-pass validation. Three further slices then took the **abstractions** those validations had left behind — an in-memory ledger, an abstract randomness source, an abstract adjudication oracle — and replaced each with a real structure, to ask whether the assumptions still hold on real plumbing, and what each assumption actually costs. What matters is that these prototypes hold each other up: one slice's measurement fills another slice's assumption, and yet another slice confirms that the assumption holds on the real plumbing. Below is an honest summary of what we verified, what held, and what still remains the province of the testnet.

## What we verified — the five validation slices

| Slice | Question verified | Method | Key result | Honest limit |
|-------|-------------------|--------|------------|--------------|
| **A. Issuance economics (`sim/`)** | Is the expected value of fraudulent issuance really negative at the draft parameters | Agent-based Monte Carlo + closed-form oracle regression | Under four attacks (adaptive, distributed, collusion, evasion) the ch7 defaults keep EV<0. **The center of gravity of the defense is detection, not the deposit** — cumulative detection converges to 1, so it holds even with the deposit driven extremely low | Four conservative model simplifications disclosed. The sole collapse point is full verifier collusion + disappearance of third-party re-checks |
| **B2. Re-execution adjudication (`verify/`)** | Is the re-execution adjudication that A *assumed* feasible actually feasible with real LLMs | Real-model experiment (Qwen2.5, SmolLM2, heterogeneous ollama) | Homogeneous runtimes adjudicate perfectly by regeneration (AUC 1.0), complementary with teacher-forcing. **Heterogeneous runtimes adjudicate imperfectly** — worst-case false-negative rate (FAR) ≈ 0.375 | A's "per-sample detection = 1" holds only for homogeneous runtimes; heterogeneity was an optimistic assumption, shown by measurement |
| **Feedback (`sim/` FAR)** | Does the economy hold once the imperfection of heterogeneous adjudication is injected | Feed B2's measured FAR (0.25–0.375) back into the A engine and re-sweep | The ch7 defaults keep EV<0 across **all 96/96 scenarios; no deposit increase needed.** Even as per-epoch adjudication leaks, the 24-epoch cumulative detection stays ≥0.997 | Adjudication imperfection modeled as a single parameter (correlated failure is a separate task) |
| **C. Payment channel (`channel/`)** | Is the honest party protected under exit fraud and offline exit in an L2 channel | Real ed25519 signatures + commit-close-dispute state machine | Latest-signed-state-wins dispute preserves the honest party's share under both attacks. **Four invariants hold over 500 arbitrary histories with 0 violations — each history injects adversarial submissions (bad signature, unbalanced state, stale nonce), so the invariants actually fire when a validation gate is broken** | Genesis, group channels, and real networking are out of scope |
| **B1. PoI plumbing (`poi/`)** | Does the commit-sample-slash plumbing of issuance verification withstand protocol attack | Real sha256 Merkle + public-randomness sampling + slashing | All four attacks (tamper, non-disclosure, inclusion forgery, grinding) resolve to slashing. **Five invariants over 500 epochs, 0 violations — the sweep now adversarially exercises proof verification and the sample's dependence on randomness (an invalid proof injected on a non-forged leaf)**. The measured detection rate reproduces A's oracle `1−(1−f)^k` within 3σ | The actual ledger derivation of the public randomness and the re-execution adjudication are separate layers (replaced by abstractions) |

## Extending C — the group-channel slice

C validated the two-party channel. The **group channel** the whitepaper cites in 6.3 as a capital mitigation (N agents sharing a deposit) carries a risk the bilateral case does not — can N−1 collude to cut the honest party's share? G places the safe rule (unanimity) and the pragmatic relaxation (threshold signatures) side by side in the same code to demonstrate the cost of relaxing.

| Slice | Question verified | Method | Key result | Honest limit |
|-------|-------------------|--------|------------|--------------|
| **G. Group channel (`group/`)** | In an N-party shared-deposit channel, is the honest party protected against N−1 collusion | Real ed25519 (reuses axon-channel) + dual verifier (unanimity vs threshold) | Under unanimity both the colluding state and a rollback are rejected, preserving the honest party's share. **Relaxing to a threshold (k-of-N) lets the same collusion pass and the honest party loses its entire deposit — the cost of relaxation is demonstrated**. Five invariants over 500 arbitrary group histories, 0 violations (each history injects colluding, replayed, negative, and phantom-party states; neutering the gate fires them 300/300) | Unanimity stalls the channel if even one party goes offline — obtaining liveness safely (per-party nonces, virtual channels) is roadmap. The capital benefit is fungibility, not capital per throughput |

## Stripping out the abstractions — three integration slices and one end-to-end slice

The five validation slices left abstractions in three places: the L1 ledger was an in-memory dictionary (`MockL1`), the sampling randomness was a premise ("unpredictable after commit"), and the re-execution adjudication was a true/false oracle. Three integration slices replaced each with a real structure, and a fourth slice (E2E) removed the last abstraction — that the slices never actually call each other — along both the issuance and the payment path.

| Slice | Abstraction removed | Method | Key result | Honest limit |
|-------|---------------------|--------|------------|--------------|
| **D1. DAG L1 ledger (`ledger/`)** | `MockL1` → a real block-less DAG | Account-based + DAG (parent references), five Axon tx types (deposit, settle, issue, slash, transfer) | In a double-spend conflict **exactly one confirms, deterministically** (single-state fold). Only issuance grows the supply, split exactly into supplier share + verification tax. **Six invariants hold over 500 random transaction DAGs with 0 violations** (the generator produces real double-spend conflicts and the ledger confirms exactly one). Filling a gap the end-to-end slice exposed, 891 slashes and 492 settlements now actually confirm, so the forfeiture and authorization-consumption paths are exercised too — before that, not a single slash had ever confirmed across 200 seeds | P2P networking, probabilistic finality, and transaction signature authentication are out of scope |
| **D2. Randomness beacon (`beacon/`)** | Abstract seed → a real commit-reveal beacon | sha256 commit-reveal + exact grinding enumeration | Achieves unpredictability and public verifiability. But **a last revealer can select among 2^k outputs** → detection degrades from `d` to `d^(2^g)` (0.958→0.841 at g=2). Withhold penalties bound g and accumulation recovers it | A VDF to remove the residual bias is roadmap (we did not build a toy VDF) |
| **D3. PoI×adjudication wiring (`d3/`)** | Abstract `is_forged` → adjudication wired into the real poi protocol | Import poi for real and inject a B2-measured-FAR adjudicator into verdict's adjudicator slot | The integrated detection `1−(1−f(1−FAR))^k` emerges from the real pipeline (at FAR=0 it reduces to B1). **Four composition invariants over 300 epochs, 0 violations** | Adjudication is a deterministic stub reflecting B2's measured FAR (real LLM re-execution is B2's province) |
| **E2E. End-to-end issuance and payment spines (`e2e/`)** | The last abstraction — that the slices never actually call each other | Import all four for real and wire them in one process (beacon → sampling → adjudication → ledger issuance), with the three seams as explicit adapters carrying contract tests | A verified epoch mints exactly once at the anchor (supplier share + 3% tax); a forged epoch forfeits its bond and mints nothing. The sample is derived from the real beacon output (bound to all 256 bits — verified by exhaustive byte and bit flips). **Six issuance invariants and three payment invariants over 300 runs, 0 violations**. On the payment side the winner's share landing on the ledger is checked against the channel's own highest-nonce signed state — neutering the challenge fires it 106/200, an adjudicator that swaps the shares fires it 200/200 | **The ledger cannot see share-to-party correspondence** — swapping the two shares leaves the sum and party set unchanged, so all three gates pass (the seam defends it). Group-channel settlement was landed on the ledger by the follow-on slice M, which extended the escrow to an N-party set (a primary-fact invariant verifies every confirmed settlement's recipients are parties of the channel; per-party funding and dispute adjudication remain). The anchor-enforcement gap is closed by the follow-on slice W via verifier-signed attestations — inflated issuance 158/200 → 0/200 (below) |

## How it all connects — the chain of validation

The nine validation and integration slices pass assumptions and measurements back and forth to form a single chain, and E2E finally wires that chain into one process (C's group extension G stands outside this chain as an extension slice).

- **A assumed two things** — that the detection probability of sampling is `1−(1−f)^k` (the sampling oracle), and that re-execution adjudication is perfect.
- **B1 realized the first assumption.** When we built the plumbing with real Merkle commitments and post-commit public-randomness sampling, the measured detection rate over many epochs reproduced exactly the closed form A had premised. **A's economic conclusion stands not on an abstraction but on the real plumbing.**
- **B2 refuted the second assumption.** In real-LLM re-execution, heterogeneous-runtime adjudication was not perfect (FAR 0.25–0.375). We injected that measured value back into A as **feedback** and re-swept — and even with imperfect adjudication, repetition and accumulation catch it downstream, so the economic defense still held.
- **C realized the payment path (Chapter 6), and B1 the issuance path (Chapter 7)**, showing by invariants that the dispute and slashing rules the whitepaper had deferred to "fixed at the implementation stage" actually protect the honest party.
- **D1, D2, and D3 stripped out the remaining abstractions.** D1 put a real DAG where `MockL1` had stood, showing double-spend prevention and value conservation by invariants; D2 built the randomness B1 had premised as "unbiasable" as a real beacon and quantified **what that premise costs** (last-revealer grinding); and D3 wired B2's measured-FAR adjudication into B1's abstract adjudication slot to confirm the two slices compose correctly.
- **And D3 refined the feedback in turn.** The feedback sweep rolled adjudication failure once per epoch, but the real protocol re-adjudicates each sampled job independently. So the real detection rate is **higher** than the value the feedback used — meaning A's economic conclusion was a conservative estimate that understated detection relative to the real wiring.

- **Finally, E2E actually connected the chain.** D2's beacon drives B1's sampling and that adjudication becomes a real issuance on D1's ledger; C's channel dispute becomes a real settlement on the same ledger — all wired in one process. And connecting it revealed things only visible once connected: the ledger cannot enforce the issuance anchor, and it cannot see share-to-party correspondence either. If the value passed between adjudication and ledger binds neither the amount nor the attribution, it leaks at the boundary even when every slice is individually correct. The former was then closed by the follow-on slice W with verifier-signed attestations, moving anchor enforcement into consensus (inflated issuance 158/200 → 0/200).

In short, B2 measures reality and feeds A, B1 realizes A's sampling mechanism and confirms A, C and B1 guard the plumbing of both paths, D1, D2, and D3 replace the abstractions beneath that plumbing with real structures while presenting the bill for each assumption, and E2E wires all of it into one process to expose the defects that exist only at the boundaries.

## The four most important findings

1. **Security depends on detection, not on the size of the deposit.** The intuition is "grow the deposit and you are safe," but the simulation pointed to a different center of gravity. Issuance is a repeated game, so independent sampling repeats each epoch and the cumulative detection probability of a node that continues forging converges to 1. Thus the expected value stayed negative even with the deposit driven extremely low, and the only point where the defense actually collapsed was when verifiers all colluded and third-party re-checks vanished. **What bears the load of the design is not the collateral but the random assignment of verifiers and the openness of challenge.**

2. **Heterogeneous-runtime adjudication is imperfect, but the economy holds anyway.** Reproducing the same inference across different hardware and runtimes is hard because of floating-point nondeterminism, and a measured false-negative rate existed (worst-case ≈0.375). This refutes the optimistic assumption but is not a catastrophe — a single epoch's adjudication failure is caught in later epochs, the per-trial cumulative detection exceeds 0.997, and once caught in the end, deposit forfeiture overwhelms the gain. **Even with the measured condition injected, the economic defense of the issuance path holds.**

3. **Every time we made an abstraction real, the assumption presented a bill — and both bills were payable.** When we built the sampling randomness the whitepaper had premised as "unbiasable" as a real commit-reveal beacon, the participant who reveals last could choose among a bounded set of outcomes by deciding whether to withhold its own commitment — detection drops from `d` to `d^(2^g)`. In exchange, a withheld commitment is itself detectable and slashable, so that choice carries a price, and the accumulation of the repeated game recovers the loss. There was also a bill in the opposite direction: the fact that the real protocol re-adjudicates **each** sampled job **raises** detection above the coarse approximation the economic simulation used. **Making an assumption real moves some numbers the wrong way and some the right way — we wrote down both.**

4. **We adversarially audited whether the invariants actually verify anything.** The security properties of the ten slices are verified not through a few scenarios but as **invariants** over hundreds to thousands of arbitrary histories, epochs, and transaction DAGs. But "0 violations" is weak evidence on its own — an invariant that catches nothing also reports 0. So we audited each slice's invariant sweep by **deliberately breaking the code** (neutering the validation gate or deleting an individual check) and re-running the same seeds, counting how many seeds each invariant actually fails on. At first several sweeps passed even with the gate removed entirely — because the generator produced only honest inputs and never exercised the validation path. We fixed this at the root by injecting adversarial submissions into each random history (bad signatures, unbalanced states, double-spends, stale-state replays, leaf-count forgery, sample tampering). Now breaking a gate makes invariants fail on tens to hundreds of seeds (channel validator neutered: 135/200; ledger dropping a valid tx: `dag_soundness` 109/200; sampler ignoring randomness: 300/300). We also fixed the real defects an adversarial review caught before merge — a cooperative close replaying a stale state (domain separation), a Merkle tree not binding the leaf count (binding n into the root), a settlement path accepting a negative share (outright rejection), and tautological invariants in PoI proof verification and ledger DAG soundness (replaced with independent recomputation). **That verification passed means it could have failed and did not — and we proved it could have, in seed counts.**

## Honest limits

These ten slices are all **pre-testnet prototypes** and do not substitute for measurement. Specifically:

- **Both paths are now one process — except for group channels.** The E2E slice wires beacon → sampling → adjudication → ledger issuance, and the payment spine wires channel dispute → ledger settlement, so the earlier limit — "the slices never actually call each other" — is resolved **across the whole bilateral path**. What remains is group-channel (G) settlement, and it remains not for want of code but because **the ledger's escrow schema is bilateral** (next item). And wiring end to end is not the same as wiring a network: P2P remains roadmap, and a system with many real nodes running is the province of the testnet.
- **Group-channel settlement was resolved by extending the ledger to N parties (per-party funding and dispute adjudication remain).** The schema limit the payment spine surfaced — `deposit` builds a 2-party escrow and `settle` restricts payout to those two — was closed by the follow-on slice M, which extended the escrow to a sorted N-party set. An N-party group settlement now confirms as-is (2-party is the N=2 special case), and whether a confirmed settlement's recipients are all parties of that channel is verified by a primary-fact invariant that compares against the member set the generator recorded at deposit — inject a real defect into the shared gate and only that invariant fires (99/100) while the other eight stay silent. Two things remain: multi-party funding, where each participant stakes their own share, does not yet exist (a single sender still locks the whole total), and the group dispute adjudication belongs to a layer outside the ledger.
- **Anchor enforcement moved from node policy into consensus, and the single verifier key was extended to a k-of-N committee.** The gap E2E surfaced ("the ledger cannot enforce the issuance anchor") was closed by the follow-on slice (W). Replacing the opaque token with a **verifier-signed attestation** (ed25519 over the amount and issuer, verifier public keys registered at genesis) and deleting the node relay policy (`admissible`) entirely, inflated issuance went 158/200 → 0/200 — and disabling the ledger's signature check reproduces 158/200 exactly, measured proof that enforcement is now inside consensus. A further slice (K) then extended the single registered key to a **k-of-N committee multi-signature**: authorizations carry a signature set `((verifier_id, sig), …)`, and the ledger issues only when at least k **distinct** valid signatures from registered verifiers are present (a duplicate counts once, an unregistered or mismatched signature counts zero, so a k−1 collusion is rejected; all ten constructed attacks are rejected by the ledger; N=1,k=1 reduces to the prior single signature). The pre-merge review returned **SOUND for the first time with no blocking defect**, and the `committee_threshold` invariant is proven non-mirror — lowering the gate to accept one signature fires it 100/100, while the state-reading mirror fires 0/100. Footnote: an empty committee (threshold 0) would allow an unsigned issuance, blocked by clamping the effective threshold to `max(1, k)`.
- **The next slice (S) turned the committee into a draw, and measured that the draw is breakable — that finding is the slice's main result.** Verifiers register via `stake_tx`, locking a uniform bond; each epoch a committee of C is drawn from the whole registry by ranking `sha256(seed || verifier_id)` and taking the lowest C; the ledger counts only those members' valid signatures (registered-but-undrawn contributes zero, and an epoch with no seed rejects issuance). End to end, the same beacon round that drives sampling also draws the committee. **That much is solid**: capture probability matches the hypergeometric tail within 3σ (n=20, C=5, k=3, 20,000 draws); the `committee_membership` invariant re-derives the committee locally from the generator's primary-fact seed, so regressing the gate to registry-wide acceptance fires it 100/100 while the mirror form fires 0/100; and three mutants — a constant beacon output, a widened committee, a truncated draw — each kill tests. **But the ranking input contains a value the adversary chooses for itself — the verifier id — and the seed is public before the fold.** An adversary registering after seeing the seed searches for ids that outrank every honest verifier and takes seats **deterministically**: against five honest verifiers, 18 hashes secured 2 of 3 seats and minted half the genesis supply for a bond cost of 200. The cost is linear in registry size, hence effectively free, and **an honest beacon does not help** — the attack needs late registration, not a manipulated seed. The most important observation: **all ten invariants hold under this attack.** The ledger obeyed its own derivation rule exactly, and invariants measure compliance rather than the rule's soundness — the invariant family is structurally blind to a defect in the rule itself (lesson L5). The hypergeometric table is therefore a target under the assumption that the registry closes before the seed is public, not an achieved defense, and the **eligibility cutoff** that would enforce it (fixing eligibility before the randomness is knowable, which needs an epoch ordering the ledger lacks) is the next slice. The attack ships as the `sortition_id_grinding` scenario so the number is published, with tests pinning both its success and the invariant suite's blindness so a future cutoff breaks them loudly.
- **The next slice (E) made that cutoff a ledger rule — and its own review then enlarged the residue again.** An epoch is opened by an authority-signed transaction carrying the seed, and the eligible set is the **ancestor closure** of that opening (∩ confirmed). Ancestors are committed through the parents into the tx-id, so nothing enters an opening's past once it exists — whereas fold order can be moved by grinding `topo_order`'s min-tx-id tie-break, which is why it could never ground eligibility. **The same 18-hash grind now buys 0 seats** and the solo issuance is rejected. The invariants gained sight here: eligibility is recomputed from the raw DAG ancestry, so neutering the rule fires `committee_membership` 100/100 (with `attestation_bound` co-firing), and a new eleventh invariant `epoch_opened` fires 100/100 when the opening requirement is removed and **alone** in 63/100 when the `duplicate epoch` guard is dropped. A review also measured and fixed that without binding the parents into the opening's signature, anyone could copy it and steal the whole eligible set. **But the final pre-merge review enlarged the residue again:** (1) issuance single-use is per **(epoch, supplier)**, not per epoch, and supplier is unauthenticated — so a committee capturing one epoch mints **without limit, and indefinitely since epochs never expire**: twenty issuances took supply from 100,000 to 20,100,000 while **all eleven invariants held**. The hypergeometric table therefore prices the **probability of capture, not the loss given capture.** (2) Censorship is not the authority's alone — a third party can invalidate the transaction an opening hangs from with one transfer and erase the epoch, after which recovery needs the authority's key. Both ship as scenarios. Outstanding: a per-epoch issuance cap with expiry, and beacon-on-ledger so the authority cannot choose the seed.
- **The next slice (P) made the loss finite with a cap — and the place it declared fixed was breached again by the final review.** Genesis seeds a per-epoch cumulative ceiling, the epoch record accumulates confirmed issuance, and the gate sits after signature verification and before any state change, so an over-cap attempt burns neither the authorization nor a nonce. **The same twenty-issuance attack now halts at the ceiling** (5 confirmed; supply 100,000 → 105,000). A twelfth invariant sums confirmed issuance per epoch directly from the raw DAG — reading the ledger's own bookkeeping back would be a mirror — and fires **100/100 and alone** when the ceiling is raised, in contrast to the co-firing seen in every S and E teeth run. **But the review found the same defect twice, at two different depths.** Opening keys on `epoch_authority`, not `committee_size`, so an "authority but no sortition" genesis created epochs with no cap at all and confirmed 999,999 against a supply of 10,000. That configuration was fixed to **demand** a cap — and in that mode nothing required the epoch to *exist*, so **an issuance naming a never-opened epoch id skipped the gate entirely**: slice E's attack reproduced in full in front of a live cap (20/20 confirmed, supply two-hundredfold, the epoch table empty). Epoch existence is now a precondition wherever a cap is configured, and invariant 11 reads the genesis authority instead of `committee_size`, so that mode is no longer a blind spot (lesson L7).
- **What the cap does not bound is recorded alongside it**: the cap bounds **one epoch**, not the ledger — the authority decides how many epochs exist, and 10 epochs × 5,000 = 50,000 was minted with all twelve invariants holding. The ceiling is a policy constant, not measured verification work, and its value lives in an unauthenticated, min-tx-id-selected genesis field (a genesis carrying a ceiling of 10¹⁸ won on a handful of grinds). And the cap makes epoch quota a first-come-first-served resource, so which of two honest suppliers is evicted is decided by that same movable tie-break — the mutual eviction slice W removed by putting the supplier in the replay key, returning on the amount axis. The hypergeometric table therefore now prices the expected loss of **one epoch**.
- **The next slice (A2) established ownership — and this slice's largest result is the fact that the defect went unpublished for ten slices.** `apply_tx` checked only `balances[tx.sender] >= amount`. A third party holding no keys built a transaction in the victim's name, **took the entire 100,000, and it confirmed — with all twelve invariants silent**, because the ledger obeyed its own rules and the rules had no ownership in them. The fact appeared in none of `ledger/README`'s limits, none of this summary, none of the whitepaper, none of the report. One limit mentions the missing sender authentication, but in the context of *registry authenticity*, which hid the larger fact behind it. An account address is now the first 128 bits of `sha256("AXNADDR" ‖ pubkey)` — **deliberately wider** than the 64-bit tx-id, because a tx-id collision is an accident and an address collision is a theft — constructors take a **private key** rather than a sender string, so building a transaction in someone else's name means calling a function named `forge`, and the authentication gate runs **ahead of** the nonce check on every folding path.
- **But the substance of this slice is that it ran four adversarial reviews, and all four returned the same diagnosis: the named instance was fixed and the rule was not.** Round 1 found tx-id slot squatting — a regression this slice **opened**, since putting `auth` outside the tx-id let an unsigned copy squat an honest transaction's slot. Closing it, round 2 found the same attack on the **epoch-id slot** (a third party copies the authority's signature into its own opening: **6,724 of 8,024 confirmed transactions dropped, 100/100 seeds, all thirteen invariants silent**). Round 2 applied "invalid input is a reason, not an exception" to two fields; round 3 found the other **ten** (one transaction from a stranger with no balance killed **200/200 seeds across both sweeps** with **zero invariants firing** — `confirm()` dies, so the invariants never run). Round 3 was answered with a field-shape table; round 4 found **`tx_type`, the table's own index**. That last one is the sharpest: **an adversary generated from a table can never test that table's index**, because its value space is the table's image — so all five omission locks were blind in the same direction. The four reviews found **22 defects** between them and **16 fix commits** answered. Also closed along the way: a third party settling someone else's escrow, a duplicate verifier-key registration that made one signature count twice (supply 100,000 → 150,000), a forged genesis that killed the whole fold, and a slash authorization replayed under another name.
- **What is not closed ships as measured scenarios, not prose.** Addresses are free to mint, so replay slots are still unbounded in count (the cap bounds that, not authentication). The opening authority still buys 2 of 3 seats for 18 hashes. A channel id is burned permanently at **zero net cost**. An admissible forged genesis still takes the selection. And the most structural one — **all thirteen invariants measure only over-admission.** Under-admission, an honest transaction being *dropped*, has nine enumerated mechanisms and **not one row where any invariant fires** (lesson L8).
- **The next slice (B2L) removed the epoch-opening authority — and its conclusion is not a success but three honest records.** The actor who opened epochs is gone. `epoch_tx` and the genesis `epoch_authority` are deleted, replaced by **commit–reveal rounds** published by registered verifiers. A round **seals** when its confirmed reveal set equals its confirmed commit set, and the seed is the **content** of that set rather than anyone's choice. The eligibility cutoff moved forward to the **commit phase** — at commit time no envelope is open — and the round closes **structurally**, using neither a clock, nor a quorum adjudicator, nor fold order: a commit whose ancestry holds a reveal of that round is rejected. A fourteenth invariant, `beacon_sound`, re-derives each confirmed epoch's seed **from the raw DAG's reveals** and fires 100/100 alone under three separate mutations.
- **But the eligibility cutoff slice E published as closed survived neither candidate rule.** Under the shipped rule (eligibility = union of the commits' ancestries) an adversary stakes two addresses on a side branch and runs a **private round** — eligibility is its own two, the draw degenerates, both take seats, and **50,000 is minted for 200 in bonds with zero grinding.** Round ids are free strings, so **twenty rounds reach a supply of 20,100,000** — bit-for-bit the number this repository cites as the pre-cap disaster. The cap bounds an **epoch**, not an attacker. The alternative rule (eligibility = the registry at seal time) was implemented and measured: the private round dies, but the **last revealer computes the seed and then registers**. Both cost about 200 in bonds.
- **And the reason given for rejecting that alternative was false.** It was rejected because it "reopens what slice E closed" — and the pre-merge review measured that the same hole is open under the **shipped** rule too. That scenario had built the entire attack and simply never had the adversary **commit to the round**; adding two commits on the same side branch takes eligibility 5 → 7, seats 0 → 2 of 3, the solo mint from rejected to confirmed, and supply to 150,000. The justification is withdrawn, and the honest reason for keeping rule A is now recorded: its eligibility is tx-id-bound ancestry, which an invariant can re-derive locally (which is why invariant 10 has 100/100 teeth), whereas the alternative rests on fold order and moves a hole of the same size somewhere the invariants see it less well. **Neither closes the cutoff; what is missing is a clock.** That, and both rules' measurements, are recorded in the report and the spec — keeping a rejected design together with its numbers is this repository's discipline (lesson L9).
- **The methodological lesson this slice left (L6)**: three of the four checks meant to verify the eligibility rule **passed with the rule deleted** — in a linear chain, ancestry and fold order give the same answer. To measure rule A, build the case where rule B cannot produce the same answer (fork the late stake onto a sibling branch and grind its tx-id so it folds *first* yet stays outside the opening's past).
- Two limits survive both slices: registry authenticity rests on an unauthenticated genesis and unauthenticated stake transactions (transaction sender-signature authentication is out of scope, so any funded account id can register, and registration is first-come, permitting an honest verifier's id to be squatted); and the ledger cannot check that the signed amount equals the work actually verified — only the prototype-level invariant `issuance_anchored` (136/200) spans that gap.
- **The next slice (CLK) gave rounds a monotone number — and the headline is not "capture is closed" but "the eligibility axis is closed and the seed axis is measured and published"; what this slice bought is one preparatory round.** Round and epoch identifiers went from free strings to **monotone integers**. `beacon_commit` / `beacon_reveal` carry a `round_index: int` that must equal `state.next_round` **exactly** (reason `round index is not the next round`), and only a seal increments `next_round`. The old reason `round already sealed` is **absorbed** into that one line and the new gate is **wider** — it blocks not only rejoining the past but **claiming the future**. And eligibility moved one round forward: eligible(n) = the **union of the ancestries of round n−1's confirmed reveals** ∪ the genesis registry, with eligible(0) = the genesis registry — so eligibility freezes **before** round n's commits exist. **That tense is the point, and the number is what buys it** — it is exactly what rule B, which B2L rejected, did not have (under B the last revealer computed the seed and then registered, buying seats for 46 hashes). The behavioral change ships with it: a verifier that registers by stake becomes drawable **one round later**. An optional genesis field `epoch_expiry_window` and the reason `epoch issuance window expired` arrived too (gate order: `insufficient valid signatures` → `epoch not opened` → **expiry** → `epoch issuance cap exceeded`), and **unlike the cap it is not declared mandatory** — an epoch without a cap is an unbounded loss, whereas an epoch without an expiry is a hygiene problem.
- **The invariants went from fourteen to fifteen, and the new fifteenth sees the other half of B2L's pre-merge review finding F4.** What F4 measured was: "no invariant guards the rule that keeps a seal final — invariant 12 catches an `issued` rewind, but **nobody watches the half where the seed and the eligibility diverge**." The new `round_sequence_sound` **re-derives** from the raw DAG that ① the set of confirmed epoch numbers == {0…K−1} and ② each n's eligibility == the ancestries of round n−1's confirmed reveals ∪ the genesis registry, reading neither `state.next_round` nor `pending_eligible` nor `epochs[n]["eligible"]` and never calling `eligible_verifiers`. In a teeth run that **causes a real re-seal**, only the fifteenth fires, **alone, 100/100**, while `beacon_sound` stays silent — the latter re-derives the seed from the final confirmed set, so once the re-seal settles the gate and the oracle agree again. Closed alongside it: **the path that privatizes eligibility with a single private round** and **opening R rounds at once**, plus four axes of §8 ("an invalid input is a rejection reason, not an exception") — the magnitude of an integer (`MAX_INT_DIGITS = 640`), the depth of a structure (`MAX_CONTAINER_DEPTH = 4`), the cost of a traversal (`MAX_TRAVERSAL_STEPS = 1024`), and rendered bytes (`MAX_RENDER_BYTES = 524_288`).
- **The price list ships as numbers, not prose.** One preparatory round = **4 transactions · 0 grinds · 0 additional bond**. Pay it and eligibility goes 0 → 2, seats 0/3 → **2/3**, supply 100,000 → **150,000**. **The seed axis does not close** — an early sealer becomes that round's **only revealer**, so even with eligibility spanning all **7** registrants, **11 salts** buy 2 of 3 seats. **The number does not forbid repetition; it serializes it**: at a cap of 1,000,000 with R=20 the supply reaches **20,100,000** (bit-for-bit B2L's number) at a cost of 4 transactions per round. **One grind of the genesis registry** skips the preparatory round in **0 rounds** (0 bond, 6 transactions, 2/3 seats, supply 150,000). And **an early seal is capture and blanket censorship at once** — even with all 5 honest verifiers committing, eligible(1) = the adversary's 2, so **all 5 honest stakers are excluded from the next epoch's committee**.
- **The worst thing this slice newly opened is an escalation in the halting grade.** Withhold one reveal and the old rule stalled only **one named round**; now that the number serializes rounds, **the whole ledger** stops. **Recovery paths 1 → 0.** Worse, **the honest side's only available action** — a verifier that has not yet committed joining in — grows the commit set 3 → 5 and pushes the **sealing condition further away**. **The expiry window has no invariant** — it depends on fold-time state (`next_round`), and imitating it through ancestry lets the issuer pick parents to neuter it. Making it a primary fact via an `at_round` field was rejected at design time (an adversary that under-reports and keeps its ancestry thin makes it silent, so a partial invariant becomes a value that only pretends to exist). The **tenth** §8 axis — the size of the string an adversarial `__repr__` returns — would require changing `_content_repr`'s explicit contract (tx-id stability) to close, so it **ships as a design decision**.
- **Three rounds of adversarial review each returned a Critical, and all three were §8** — the magnitude of a value → the cost of a traversal → rendered bytes. All three arose **on top of a green suite**, and all three **satisfied exactly** every rule in force at the time. Just as an adversary grown from a table cannot measure that table's own index (lesson L12), a lock that closes one axis is blind to the axis beside it.
- **The determinism check became reproducible only in this slice.** `python -m ldg dump` **went into the repository** — before that the script that built the dump lived outside it, which made one published headline verification impossible to reproduce (lesson L10). At `PYTHONHASHSEED` 0, 1, and 2 the three dumps are each **1,400,863 bytes** with sha256 `d39bfe39…`, identical, over **7,880** confirmed / **13,257** rejected (80 DAGs). The weak check (3 seeds × 2 sweeps × 100 seeds × 15 invariants) reports **0** violations. ⚠️ **The byte count and hash are not compared against the previous edition (1,561,262 · `08ede27c…`)** — the old script was not in the repository, so the same rendering format could not be restored. **Only the confirmed/rejected counts are a property of the ledger rather than of the script**, so only those carry over: not one confirmation moved, and rejections rose by exactly **+720** (= 9 arms × 80 DAGs).
- **The next slice (SLH) turned that 0 into a 1 — the headline is "withholding went from free to two bonds," and the subtitle is "forbidding self-forfeiture is a price, not a barrier."** A ninth transaction type arrived: `reveal_default(round_index, defaulter, witness)`. A **witness** is a transaction the defaulter signed and got confirmed whose ancestry already holds a confirmed reveal of that round — it proves liveness and non-cooperation **at once**, and **a slasher cannot build one**. There are six gates; only the first (`witness does not prove selective absence`) reads ancestry and therefore lives in **`confirm`**, while the other five (`round index is not the next round` · `not a registered verifier` · `sender is the defaulter` · `not a committer of this round` · `already revealed for this round`) live in `apply_tx`. The effect is `tax_pool += stakes.pop(defaulter, 0)` · `verifiers.pop(defaulter)` · `del beacon_commits[(round, defaulter)]`, so the commit set shrinks, `committed == revealed` holds again, and the round **seals**. A sixteenth invariant, `bond_forfeiture_sound`, fires **100/100** on teeth 16a/16b (the conservation invariants stay silent — evidence it is not a restatement of invariant 1) and **0/100** on two mirror mutants. **Recovery paths 0 → 1**: one forfeiture takes commits 3 → 2, epochs `[0,1]`, `next_round` 1 → 2, a confirmed issuance of 500, and supply 100,000 → **100,500**. The old values are kept as history — the round never sealed, `next_round` stalled forever, zero confirmed issuance, recovery **0**.
- **The whole price list is measured and published, and its first line is "this slice opened no new axis; it priced an axis already open."** The g-collusion curve fits the **same closed form** as `prepared_commit_grinding` — at distinct seed counts `[1,2,4,8]` the capture rates are **0.133 · 0.245 · 0.448 · 0.657** (hypergeometric baseline 0.143). That path is unlimited and free and reached a capture rate of **1.0000**, whereas this one stops at 0.657, so the SLH path is **dominated by the free path already open and does not enlarge the adversary's capability.** **One bit of the seed costs two bonds** — two seeds differ in **31 of 32 bytes** (`4b30cb85…` vs `c1438764…`), seats go **1/3 → 2/3**, an issuance of 50,000 goes from rejected to confirmed, and supply 100,000 → **150,000**. The price is 100 forfeited + 100 locked across 4 transactions, and the locked bond is unrecoverable because the nine `FIELD_SHAPES` entries contain **zero `unstake`**. **Forbidding self-forfeiture is a price, not a barrier** — the detour was measured **before** the gate was added, and a defaulter that registers a second address to forfeit itself buys **the same one bit** (arm W2 does not differ from the self-forfeit arm **in a single value**). The price merely went from 1 to **2**.
- ⚠️ **Those "two bonds" are the price of acting "alone, once," and sequential repetition was not measured.** The locked 100 is not burned; it survives as the second address's asset, and that address lives on as a registered verifier. If it becomes the next round's defaulter, the locked bond becomes the next burn, so **the marginal cost converges to one bond per bit** — derived by reading the rules, not measured.
- **Total silence cannot be slashed.** A party that neither reveals nor transacts leaves no witness that **can exist** — we fed **all 26** confirmed transactions in as witness candidates and measured **0 passing**. Telling that apart from a crash needs a real clock, and that is outside this slice.
- **The slasher has no incentive — this rule presumes altruistic participants.** The change in (balance + stake) across all 5 registered verifiers is **0**. The forfeited bond goes to `tax_pool` and no rule reduces it. The **dead seat** ships alongside: seats freeze at the seal and forfeiture comes after, so a forfeited party keeps its seat and loses only the ability to sign — in e2e the old signer policy drew it and **the honest spine issued nothing** (supply 10,000 unchanged; drawing from live seats gives **10,064**). The policy was fixed, but that only **defers it by one**: a C=3, k=2 configuration survives exactly **one** forfeiture.
  > ⚠️ **[Correction — slice FEE, 2026-08-23] Half of this sentence became false, and fixing it is what the next slice did.** Forfeiture still pays nobody — that inflow to `tax_pool` still has no exit. But **a seat is now worth something**: the issuance tax is split evenly among the verifiers who attested that issuance. Re-measuring the same wiring, **one signer earns 7**, and the slasher is one of the two — so the slasher's income is not 0 either. **It comes from the seat, not from the forfeiture.**
- **Three residuals ship as values too.** Forfeiting a genesis registrant buys **only the loss of registration** (0 forfeited — the control, a stake registrant, gives 100). **The loss of registration is not permanent** — forfeiture releases both `verifier already registered` and public-key uniqueness, so the same address registers again; not forbidding it is a design decision (blocking the same address is no barrier, since a new address registers on the same bond). The price is **one new bond**. And **two of the six forfeiture gates fired 0 times in the sweep** — `sender is the defaulter` and `not a committer of this round`. No adversary was planted to aim at them, and planting one would put both sweep totals up for full re-measurement, so we planted none and recorded the fact.
- **The pre-merge adversarial review (three axes in parallel) returned Critical 2 · Major 5 · Minor 8, all fixed — and the heaviest was that the clause carrying the published headline on its own had no teeth at all.** M1: delete witness condition ① (`witness not in confirmed`) and a slasher burns an honest verifier's bond and steals the round with **a single unsigned transaction it wrote itself** — and at that moment the **sweep was 0/60 with all 417 tests passing**. Planting an adversary took it **0/20 → 20/20**. Both Criticals were **false positives on an honest ledger** — C1, because invariant **10**'s registry did not subtract the forfeiture, fired `committee_membership` even with 0 rejections, a confirmed issuance, and supply 100,500; C2, because `_local_eligible_for`'s subtraction was permanent, could not undo a **re-registration**, so both variants were false positives. The rest: a price contradiction in the published copy, a stale copy in `report.py`, and a D9 correction block **that was itself false**. **What survived**: §8's **nine axes over 3,394 cells with 0 exceptions** (a legal worst-case 131KB forfeiture still confirms in 8ms), `confirm` and the oracle being the **same function**, invariant 16 being **no mirror**, the six reason strings matching **6/6 character for character**, and the price list being **honest** (exhaustively confirmed that forfeiture is the only code that reduces `stakes`).
- **The determinism figures are updated to the SLH edition — the CLK values directly above are kept as history.** The three dumps at `PYTHONHASHSEED` 0, 1, and 2 plus **two runs at the same seed** are each **1,478,815 bytes** with sha256 `ec7f6d0e…`, all byte-identical (previous edition 1,400,863 · `d39bfe39…` — since `python -m ldg dump` lives in the repository, these two are comparable end to end). The **8,240** confirmations **did not move by a single one** in this slice, and rejections went 13,897 → **13,977**, decomposing exactly as **+80 = 2 cases × 40 sortition DAGs**. ⚠️ This slice's difference from the CLK edition (7,880 / 13,257) to the state just before SLH (8,240 / 13,897) **was also measured here**: the **+640 rejected decomposes exactly** — inside the dump, `witness does not prove selective absence` appears **600 times** and `already revealed for this round` **40 times**, the two reasons raised by the two forfeiture adversaries the ninth transaction type planted. ⚠️ **The +360 confirmed was not decomposed** — the dump's confirmed list records tx-ids only, not types, so it cannot be split there; we record the measurement and do not guess at the mechanism (lesson L4). The pre-gate grid is **8,448, unchanged**, the transaction types went from eight to **nine**, and the invariants from fifteen to **sixteen**. The suites are `ledger/tests` **510 passed · 3 skipped · 0 failed** (513 total) and `e2e/tests` **78 passed**.
- **The next slice (TIME) put a time surrogate at the commit stage — the headline is not "an axis was closed" but "the quorum `q` does not close an axis, it prices one — and the crossing point `q=3` we first published is withdrawn."** A genesis field `seed_quorum` (=`q`, default **1**) arrived and the seal forked in two. `S(n) = { commit c : at least q of this round's confirmed reveals have c in their ancestry }`, and if `|S| >= q` the seal is **normal** (the seed is S's reveals, and a committee is drawn); otherwise it is **degraded** (`seed = None`, committee `None` — yet the epoch is still created and `next_round` still increments). The count is written into `state.seed_support` by `confirm` as it folds a reveal and rolled back if the reveal is rejected, and it attaches **to the commit currently registered, not to the sender**. What decides whether a commit enters the seed is no longer "what does it have in its ancestry" but **"what has it in *their* ancestry"** — the one direction an adversary cannot forge. **No new transaction type**: one genesis field.
- **Three axes ship in one table, and that table is this slice's conclusion** (measured at N=5, C=3, k=2). ① **Buying the bias** (the late envelope) — threshold `m >= q`, price **0 forfeited + 100 x q locked**, 0 rejections, **0 of 16** invariants firing; raising `q` makes it **more expensive** and there is nothing to undo. ② **Withholding** — threshold `w* = N - q + 1`, price 100 x w forfeited + **w registrations lost**; raising `q` makes it **cheaper**, and forfeiture restores **only the seal**. ③ **The detached reveal** — threshold `k* = N - q + 1`, price **0**, 0 rejections, **0 of 16** invariants firing; raising `q` makes it **cheaper** and there is **no** undo. **Why the recommendation is withdrawn**: the crossing put ①'s **bond count** and ②'s **head count** on one axis, and the two are **not the same unit** — and ③ has **the same threshold as ② at a price of zero** with no undo, so the availability side of the trade **can be bought for free and there is nothing left to trade.** Raising `q` makes one priced axis more expensive and **one free axis cheaper.** ⚠️ **What is withdrawn is the recommendation, not the observation** — "there is one cell where the two numbers coincide" is a measurement and is not deleted; only the reading that says *pick that cell* changes. The main-line judgment `q=1` (the rule off by default) is **more strongly** supported by the third axis, not less.
- **And `q`'s teeth are causal, not economic.** One unit of support costs one bond only because the adversarial commit sits in the honest reveals' **future**; the same commit sitting **inside their past** takes support to 6 on a single bond and passes all the way to `q=5`. A late commit cannot enter that past **at any price**. The correct reading is not "`q` bonds buy it" but **"buying the seed *in the honest reveals' future* costs `q` bonds."** ⚠️ **Salt grinding works on that arm only because the simulator makes honest secrets deterministic and exposes them** — in a real system that commit would have to be built blind, so on arms where several revealers mix, the published salt counts are **optimistic**.
- **The pre-merge review returned this repository's heaviest harvest — three parallel axes yielding Critical 2 · Major 8 · Minor 6, and both Criticals were invariants raising false positives on an honest ledger.** There is one root: this slice moved the seed from "all confirmed reveals" to "the reveals that pass the `S(n)` filter", **three places re-derive that rule**, and the migration went **to the gate only**. Invariant 14 `beacon_sound` raised a false positive **at the shipping default `q=1`** (an honest ledger degraded-sealed by a forfeiture), and invariant 10 `committee_membership` raised one wherever `q>=2` actually filtered. Throughout, **all 546 tests in the suite were green.** The fix was not to copy but to fold — one derivation in `_local_round_seed`, one rollback in `bump_seed_support` / `restore_seed_support` (lesson **L14**: move a rule and you must move **every place that re-derives it**, and what goes stale is not only numbers but conclusions). Closed alongside: the ghost count (a reveal folding *after* a forfeiture holding the already-deleted commit in its ancestry), a forfeited verifier re-committing in the same round, an unguarded `None` where `q=3` killed report generation outright, and three published sentences about the degraded branch that were wider than the rule.
- **One thing proved by measurement to be unguardable, and one thing shipped as an unclosed limit.** The first — `q > the committer count` is a **dead ledger** (every sealed round degraded, **0 rejections**, issuance permanently `epoch not opened`), yet the sortition genesis registry is **empty** and `n` is a fold-time value, so the genesis cannot decide it. The other four dead configurations (`committee_size < max(1,k)`, `epoch_issuance_cap <= 0`, `verifier_bond <= 0`, `seed_quorum < 1`) are all rejected at the genesis, so having guards there and none here is a **reason, not an omission**, and a regression pins the boundary. The second — `committers_behind` walks the full ancestry **ahead of** `apply_tx` (authentication and shape). **One** forged reveal pays for the whole DAG (**481.0 visits per case**, order `0.750·N²`) and **buys nothing**. **Both shortcuts measured to zero**: putting authentication first does not help (a reveal self-signed from any address pays **the same 481.0**), and the forged-commit face was already created by B2L's gate — what TIME changed is **the number of transaction types that pay it (one → two)**. ⚠️ §8's `MAX_TRAVERSAL_STEPS = 1024` does not block this spot — it is a **field** traversal budget, and what is walked here is DAG ancestry.
- **All eight honest limits ship.** ① `q` bonds route around it · ② **the `q=1` degenerate case — the main line has the rule off by default**, which is not an accident but a **deliberate decision** justified by the curve itself (raising an axis that does not close, globally, buys not safety but a shifted tx-id table) · ③ inducing degradation (`w*`), and **SLH's forfeiture does not pay it back** — the "recovery paths 0 → 1" SLH published is **halved** here (forfeiture buys back the clock, not the sortition) · ④ **total silence** (at `q=1` with `w=N` there are zero reveals, so no witness can be built and **forfeiture is not merely unpaid but impossible** — that is a permanent halt) · ⑤ **the determinism sweep never walks the degraded branch (a new hole)** — the dump has **0** occurrences of `seed=none` and **all 342** sealed rounds carry hex seeds (the same kind of hole as SLH's `sender is the defaulter` at 0) · ⑥ the unauthenticated, min-txid selection of the genesis field `q` comes from · ⑦ **an unbudgeted DAG traversal** · ⑧ that salt grinding works only because the simulator exposes honest secrets. **Five unmeasured values** ship by name too: the source of the `|V|` 226 vs. 227 gap against the SLH review's R1 table; the by-type decomposition of the 8,240 confirmations; a sweep measurement of the degraded branch; the salt curve re-measured with honest secrets hidden; and **axis ①'s boundary was measured at N=5 only** (`w*` and `k*` generalize — **25 of 25** cells at N=3..7, q=1..N — but the crossing is `q = (N+1)/2` and therefore exists **only for odd N**: N=4 and 6 measure `crossing []`).
- **The seventeenth determinism re-measurement — the SLH figures directly above are kept as history.** The three dumps at `PYTHONHASHSEED` 0, 1, 2 plus **two runs at the same seed** are each **1,526,328 bytes** with sha256 **`2740403d…`**, all byte-identical, over **8,240** confirmed / **14,057** rejected, and they are **compared end to end** with the previous edition (**1,478,815 · `ec7f6d0e…` · 8,240 / 13,977**, same `ldg/dump.py`). **The delta decomposes in full.** Rejections **+80 = 1 case × 80 DAGs**, and the one case is `duplicate genesis` **640 → 720** — the mechanism was measured too: `GENESIS_SHAPES` grew from **eight slots to nine** (`seed_quorum`) and `_forged_genesis_adversaries` plants one forged genesis per slot. Confirmations are **+0 — not one moved.** But **the tx-ids shifted in 100% of all 80 DAGs** (the genesis grew by one field, so everything below it re-hashed). ⚠️ **No by-type decomposition was done** — the dump records tx-ids only and the total delta is zero, so there is nothing to split (lesson L4). The bytes grew in **two stages**: first the `-- seed_support --` section at **+43,986** (schema +41,259 / content +2,727), then the review fix added a `-- forfeited_commits --` section at **+3,527** (header 24B × 80 + 40 item lines, 1,607), with **every other section at ±0**. Totals updated alongside: the pre-gate grid goes **8,448 → 8,704** (one genesis slot × 256 adversarial classes — the old value is not deleted), the scenarios **forty-seven → 51** (all PASS), the **transaction types stay at nine** (no new type; one genesis field arrived), and the **invariants stay at sixteen** — a seventeenth was **measured and not built**: six source mutations of `ldg/state.py` over 20 seeds established as a value that invariant 14 re-derives the degradation condition itself and therefore asks both directions, so a seventeenth would be a **restatement of invariant 14** (lesson L2). The suites are `ledger/tests` **547 passed · 3 skipped · 0 failed** (550 total) and `e2e/tests` **79 passed**.
- **The cost ships as a number too — what this slice added lands on the reveal arm only.** Over **807** reveals, `confirm` goes **0.318 → 0.516 s** (min, about **1.6×**), and the call happens **exactly once per reveal**. The real sweep's denominators ship with it: 80 DAGs, **22,297** transactions, **1,386** calls to `committers_behind`, **93,823** ancestors walked in total = **67.7 per call on average**.
- **The next slice (LIV) built a seventeenth invariant and then reverted it — and what ships is not the invariant but why it cannot be one.** `death_was_paid_for` — *"wherever the ledger died (① a `degraded` seal, ② a `stuck` round), at least one confirmed forfeiture aims at that round."* The motivation was that the preceding **sixteen all measure over-adoption only** — one direction, "did what was confirmed exceed the rules" — so no invariant carried the one failure whose silence is wrong: **the ledger doing nothing at all.** Three adversarial pre-merge reviews returned **Critical 5 · Major 10 · Minor 10**, and three of those findings were not in the code but **in the design**. So it was reverted (`c30f1ea` · `6838a63`). **This slice's output is the negative result itself** — and it is the most expensive worked example of what finding 4 above ("we adversarially audited whether the invariants actually verify anything") actually means.
- **The three collapsed assumptions ship with their numbers.** ① **Death cannot be decided from a fold snapshot** — `stuck` turns on **a single** confirmed reveal (one wiring, two arms, both with 0 forfeitures, `tax_pool` 0, and **0 rejections**: **26** confirmed → `[(1, stuck)]` **fires** / **27** confirmed → `[]` **silent**). And it is **structural**: a blocked round by definition cannot advance `next_round`, so it is **always at the top of the fold**, and this ledger has **no clock** with which to prove *"this round is over."* ② **`|S| < q` is not cheating** — under a **legal genesis** where `q` exceeds the committer count, a round in which everyone commits and everyone reveals with 0 rejections has `|S|=0` and is therefore `degraded` (measured grid: `n`=1 dies at `q`>=2, `n`=2 at `q`>=3, `n`=3 at `q`>=4 — every cell with 0 forfeitures and 0 rejections), **and no paying forfeiture can even be built** (no committer failed to reveal, and throwing one returns `round index is not the next round`) — an **unsatisfiable assertion** (the sortition sweep agrees: pushing `q`=4 and 5 onto seeds 0..4 yields **5 of 5** free deaths). And **paying the price buys a permanent accusation**: once a forfeiture drops the survivors below `q`, **every honest round thereafter** is a death of price zero (verifiers 3, `q`=3: simply appending one honest round at 100% participation and 0 rejections makes it fire), and that round has no withholder, so **no further payment can be bought either.** ③ **The price cannot be counted in receipts** — a **zero-price receipt** (a genesis-registered verifier is absent from `state.stakes`, so the forfeited amount is **0** and yet it buys silence) · a **half-paid death** (forfeiting only one of two withholders leaves commits 4, reveals 3, `next_round` 1 — **the round is still blocked** — and it goes silent; the `_dead_rounds` of all three arms are **identically `[(1, stuck)]`**) · **laundering** (standing **one unrelated forfeiture** beside an attack that erased the committee outright buys silence, and the arm that drove `|S|` to 0 and the arm that did not have **exactly the same `tax_pool` of 100** — that 100 is not the attack's price, so **the attack's marginal price is 0**).
- **And it was anti-monotone — increase the damage and the alarm switches off.** At withholders/committers **1/3 and 2/3** it **fires** with `[(1, stuck)]`, but at **3/3 it goes silent with `[]`** (`next_round` 1, a permanent halt). All three arms are **equally dead**, and the evasion costs **0** — not one additional transaction is spent. Even paying on that arm is impossible: the forfeiture stops at `witness does not prove selective absence`, so **the path to paying is closed too.** ⚠️ **This is not the same sentence as SLH's honest limit *"total silence cannot be slashed."*** That one is about **non-attributability**; what was measured here is **non-detectability plus evadability**.
- **Narrowing cannot save it — that is the conclusion.** By ① `stuck` is undecidable and must be **dropped**; to close ② on the remaining `degraded` the invariant must narrow to *"a death that could have been paid for"* — but **could have been paid = some committer failed to reveal.** **And in the attack this slice aimed at (TIME's third axis, the detached reveal) everyone reveals.** The narrowed invariant **cannot see the reason it was built** — widened it accuses honest ledgers, narrowed it misses its target.
- **All three headlines had zero teeth — the invariant's own teeth were measured, the sentences holding it up were not.** The invariant **itself** went **0/20 → 20/20** as a sole firing against its adversary. The three sentences above it measured **0** when the review finally measured them: *"the seal decision and the death decision share one derivation"* → the shared derivation was **two of three** (`_local_seed_set` holds a third copy and does not subtract forfeitures), and the mutation deleting that subtraction **passed all six new tests** — **that mutation is TIME's Critical itself** · *"one forfeiture cannot pay for a different round"* → a global-count mutation **passed all twelve regressions**, and instrumenting the then-**557**-test suite showed the situation **never once occurs** · *"it was folded into `all_hold`"* → deleting that one line **passed all twelve regressions**. **The population of the measurement had also drifted** — inclusion was decided on *"0 false positives in the sweep"*, while ②'s and ③'s false positives were **all outside the sweep**. The lesson kept is **L15**: if what an invariant measures is not a "rule violation" but an **outcome**, it is not an invariant — and teeth must be measured **per headline, not per invariant**.
- **The revert kept what had value.** `_round_participation` — the seal decision and the participation count folded into one derivation, and **five tests catch the mutation that deletes the forfeiture subtraction on only one side.** `_dead_rounds` was kept as an **observation helper** — `check_invariants` does not call it, and **why it cannot be an invariant is written once, in its docstring** (copy the same argument into several files and the copies drift). **`test_the_sixteen_are_silent_on_a_dead_but_legal_ledger`** pins *"the ledger is dead and all sixteen are silent"* as a **regression that ships the hole as a hole**, with `test_a_forfeiture_does_not_move_the_death_it_pays_for` beside it. **Source-string assertions were replaced by behavioral measurement** — two copy-and-rederive mutations now each FAIL (both passed in the old edition). The scenario **`a_free_death_has_no_receipt`** (four arms) has the headline *"death and price diverge across the four arms, and **all four fire 0 of 16**."* **The `detached_reveals` handle was deleted** — on the armed arm the other sixteen fired **0/40**, so it was dead code.
- **Determinism did not move by a single byte in this slice, and that is the evidence that the revert is complete — the figures were re-measured, not re-quoted from the TIME edition directly above.** The three dumps at `PYTHONHASHSEED` 0, 1, 2 plus **two runs at the same seed** are **all four 1,526,328 bytes** with sha256 **`2740403d…`**, byte-identical, over **8,240** confirmed / **14,057** rejected — **exactly** the merge-base `b8eb960` edition. The dump contains **0 lines** of `death_was_paid_for`. Totals updated alongside: the suites are `ledger/tests` **552 passed · 3 skipped · 0 failed** (**555** total — the old value **550** is not deleted; the five added are not a new rule but the **regressions and scenario** listed above; non-invariant **447** · invariant **108**) and `e2e/tests` **79 passed** (unchanged); the fast-test total goes **898 → 903**; the scenarios **51 → 52**; the **invariants stay at sixteen** (a seventeenth was built and reverted — where TIME wrote *"measured and not built"*, this slice writes *"built, measured, and reverted"*); the **transaction types stay at nine**; and the pre-gate grid **stays at 8,704** (this slice added no genesis slot).
- **Six unmeasured values ship by name.** ① **The narrowed invariant's value** — the *"death that could have been paid for"* edition was never actually built and run against the sweep. The conclusion above is an **argument at the level of definitions**, and no regression was attached to it (**the next person should measure this first**) · ② **an edition with a clock** — the only apparent way to close ①, neither designed nor measured · ③ **absolute timings before and after LIV** — while measuring, the machine was **100% saturated by unrelated processes** (the same code on the same wiring measured **0.694 s vs. 2.132 s** at `chain=800`, a 3× spread), so no before/after difference was readable, and after the revert there is nothing left to measure · ④ **the four values carried by the three teeth rows above** were measured **by the review** on the tree where the seventeenth still stood; the reverted tree has no mutation targets, so this publishing task **quotes rather than reproduces them** · ⑤ **the teeth baseline for the sixteen** (two sweeps × 100 seeds × invariant count, 0 violations) — SLH's figures are the last measurement and this slice did not re-run that table (the violation counts for the two published sweeps are emitted live by the report on every run) · ⑥ the pass counts carried in `ledger/tests/test_scenarios.py` (`48/51 · 42/51 · 37/51 · 51/51`) are **left stale** — work for the next slice.
- **The next slice (WALK) picked up the one Major that TIME could not close and shipped as a limit — the ancestry walk `confirm` performs *before* `apply_tx` (authentication and shape). The headline is not "closed" but "the order is unchanged and the slope came down 33×", and the acceptance criterion was not speed but the answer.** That walk is now **one memoized closure** (`_ancestor_facts_full` — closure = own contribution ∪ ancestors' facts, explicit stack), and the three call sites that used to ask for ancestors (`_ancestor_facts` · `commit_facts_behind` · `committers_behind`) are its **projections**. The memo lives for **exactly one fold**. **Not one character of the rules changed** — 0 new transaction types, 0 new genesis fields, 0 new invariants. The acceptance criterion was **byte-identical determinism**, and it held **at every commit on the branch**: the three dumps at `PYTHONHASHSEED` 0, 1, 2 plus two runs at the same seed are all four **1,526,328 bytes · sha256 `2740403d…` · 8,240 confirmed · 14,057 rejected**.
- **What it bought is re-measured in a single unit, and that unit is Python lines executed in `ldg/ledger.py` + `ldg/dag.py`** (chosen because it means the same thing in both trees, it counts **code that actually runs** with no monkeypatching, and it is deterministic). The marginal line count of one forged reveal is **`66·R + 67`** on main and **`2·R + 120`** on HEAD, and all five points (R=10·40·80·160·320) fit those formulas **exactly**. The multiple runs **5.2 (R=10) → 19.1 (R=80) → 27.9 (R=320)**, asymptotically **33×**. ⚠️ **Both arms are Θ(R); what came down is the slope, and the multiple itself grows with R** — so this slice's value is neither "a constant factor" nor "a better order." On the honest fold it did come down: memo nodes filled **55,307 → 935 (−98.3%)**, that count's order `0.099·N²` → roughly **`0.57·N`**, and `dag.ancestors` calls on the `confirm` path **647 → 0** — ⚠️ **reading that 0 as the headline would be a false positive** (the work did not vanish, it moved, which is why the new axes — memo nodes and merge entries — are counted alongside it).
- ⚠️ **The first headline we published, "6.0×", was wrong in two layers, and that is the seed of this slice's lesson.** ⓐ It was **a division of two numbers in different units** — the numerator was the old arm's **ancestor visits**, 481.0 per forge, and the denominator was the new arm's **merge dict entries**, 80.0 per forge. Both numbers were measured and both were individually right. What was wrong was **the division.** ⓑ That denominator was **one third of what the same loop does** — counting all five terms per forge at R=80 gives `dict entries` **80.00** + `rounds |= …` **81.00** + `frozenset` rebuild **81.00** + `senders |= …` **0.00** + `pairs[r] | ps` **0.00** = **242.00**. And the old edition's ***"what we did not measure"* list named the wrong term** — `pairs[r] | ps`, the one it confessed to skipping, is **exactly 0** on the forged arm, while the terms actually skipped were the `rounds` side and the `frozenset` rebuild (R+1 each). **The confession pointed at a zero and hid the two terms that carry the load.** The lesson kept is **L16**: what is wrong may be not the code but the **ruler**, and when it is, the suite is entirely green.
- ⚠️ **And the measurement harness did not run the real code path — two Criticals grew from that single root.** The harness that measured memory walked **every node** of the DAG unconditionally, and `confirm` does not do that (it memoizes **only the nodes the projections actually walk**). ① The published *"one fold holds a memo of 350 entries · 10,018 fact elements"* was reproduced, by measurement, to be **the median of the all-node memo** — the **fold's** memo has a sortition median of **199 entries · 5,493 elements** (**2,623** once duplicates are removed by object identity), and `fixed_committee` is **44 · 13** on all 40 seeds. ② The published *"the adversary buys O(M²) with 0 bond and 0 registration"* was **unreachable** — `confirm`'s **parent gate stands before the walk**, so a forgery with an unconfirmed parent never reaches the ancestry query and an unsigned chain survives **exactly one hop** (memo flat at **482 entries** for M=200·400·800·1,600; marginal cost per forge **1.60 → 0.20**). The arrangement that does push is the **sibling arrangement**, and that term is `4·R + 1` = **Θ(M·R)**.
- ⚠️ **So the design's §4 "economically bounded" is true again, and the paragraphs that declared it "false" were wrong.** M is the number of transactions the adversary **must actually publish**, and R is the number of confirmed rounds, which **grows only by paying registration and bond** — both are bounded. **Writing down as true something you did not measure and writing it down as false are the same violation**, and a negative declaration is more expensive to undo. This design is not free, though: the old edition had no memo, so this term was **0**, and now one forged transaction adds **R×** on top of what it already costs the DAG — that is the price this slice paid, and it ships here.
- **No cap was added, and the reason for not adding one was replaced.** The published *"a cap would itself become a new attack surface"* is **false** — the review's one-liner (*"clear the memo when it exceeds CAP"*) was actually implemented in an out-of-repo copy, and CAP **200 · 1,000 · 5,000** produced **byte-identical** 12-DAG dumps (the memo is a pure cache; clearing it only makes the next call rebuild). **The real reasons are two.** ① What `clear()` throws away is the honest chain's **re-read** entries, while the adversary's entries are leaves nobody reads again, so **the policy cannot tell the two apart** — at CAP=200 an honest fold with **not one adversary in it** touches **318.7×** the elements at R=160 and **641.0×** at R=320. ② The honest fold's memo entry count is **exactly N**, so **a cap that does not bite binds nothing** (CAP=5,000 never fired at any of the three R values). One alternative was measured too — not memoizing the walk's **root** takes the sibling arrangement from **321.00 → 0.00 elements per forge** with byte-identical dumps, but the honest chain then costs **2.16×** at R=320, and **this slice had no basis on which to choose.**
- **Three pre-merge reviews returned Critical 4 · Major 12 · Minor 10, and only two of those were code defects; the rest were almost entirely prose.** ① **A hang on a cyclic parent graph** — the old edition (`dag.ancestors`) returns immediately via `seen`, while the new explicit stack kept re-pushing any node reachable from itself forever (on a 2-cycle it was still alive after 8 seconds). Fixed, and **the old answer came back byte-identical** (400 cyclic graphs × 5,472 projection rows, **0 rows** differing). ⚠️ **An edition that silently changes the answer was measured first and rejected** — cutting the back edge with `_EMPTY_FACTS` makes the closure not the least fixed point but a function of **where the walk started**, and **497 rows differed** on the same 400 graphs. **Terminating alone is half a fix.** ② **A wiring where the order gets worse** — on a query behind `verifier_bond=1` with (transfer, stake) pairs chained, main is **1.000·N** and HEAD is **0.125·N²** (at N=8,002, **8,001 vs. 8,006,000**). The review's candidate fix (per-field reference sharing) was **actually implemented and measured: 0.05% reduction, same order** — so it **ships unfixed, as a limit.** The other two Criticals are the **unreachable O(M²) adversary** above and **M4 not being an equivalence mutation** (the published "proof" has no base case — `confirm` puts genesis into the confirmed set **without the parent gate** and `open_genesis` does not look at parents, so the same txid can have different closures in two DAGs).
- **There is not one piece of evidence that an answer changed, and three independent axes measured that.** Old loop vs. new projection: **337,722 comparisons, 0 mismatches** · adversarial values: **5,670 comparisons, 0 mismatches** · a **185-row population digest, byte-identical** (52 scenarios · partial DAGs · multiple tips · genesis with parents) · the oracle's **72 rows identical** · **all 10 planted mutations** caught · **all nine regressions** bite · **0 attempts** to mutate the shared dict. ⚠️ **What was wrong was not the code but the publication** — and that is what makes this slice the **measurement-side edition** of finding 4 above ("we audit whether the verification actually measures anything").
- **Determinism did not move by a single byte in this slice — and that was the acceptance criterion, not a failure. This slice did not change the answer, and proving that it did not was the work itself.** The three dumps at `PYTHONHASHSEED` 0, 1, 2 plus **two runs at the same seed** are all four **1,526,328 bytes** · sha256 **`2740403d…`** · **8,240** confirmed · **14,057** rejected — **exactly** the LIV and TIME editions directly above (the old values are not deleted). Totals updated alongside: the suites are `ledger/tests` **566** = **563 passed · 3 skipped · 0 failed** (the old value **555** is not deleted — the eleven added are not a new rule but the **regressions** this slice added; non-invariant collection **457** · invariant **109**, run as four parallel node-id shards of **30 · 28 · 26 · 25**) and `e2e/tests` **79 passed** (unchanged); the fast-test total goes **903 → 914**; the scenarios stay at **52**; the **invariants stay at sixteen**; the **transaction types stay at nine**; and the pre-gate grid **stays at 8,704** (this slice added no genesis slot).
- **Three items ship by name to the next slice.** ① the **M4 equivalence argument in the docstring at `tests/test_ledger.py:1691`** — measurement showed it must be retracted, but the test file was outside the publishing task's edit scope · ② the **six sites carrying *"a confirmed transaction's ancestors are inductively all confirmed"*** (`ldg/ledger.py:288`·`:574`·`:639` · `ldg/invariants.py:731`·`:988`·`:1119`) — **false, and a pre-existing defect that reproduces on main too**, but this slice **re-erected that sentence as the load-bearing argument** for the memo · ③ the **four `test_invariants.py` shards against M4** — the old edition skipped them on the (now retracted) grounds that M4 was an equivalence mutation, so with those grounds gone this item is **more open than before.** This slice also leaves **two unmeasured values**: the **wall clock of a whole fold** (the difference of two large noisy numbers — even at K=9 · M=400 the marginal cost came out **negative**, so the published wall clock of **5.1 → 38.9×** is only from the marginal query measured **in isolation**) and **persistent data structures** (the only apparent way to actually lower the stake chain's `0.125·N²`, with neither implementation nor measurement).

⚠️ **[appended — 2026-08-23] Item ② above is closed (`88b0568`), and it was a gate defect rather than a documentation one.** The induction **had no base case**: `confirm` put the genesis into the confirmed set without the parent gate, and `open_genesis` never looked at parents, so **a genesis with parents was admissible**. That let a **rejected, unsigned, bond-free transaction change a descendant's fate** — with the same genesis and the same transaction set, the presence or absence of that one forgery splits the fold into five confirmed with epoch `[0]` versus three confirmed with epoch `[]` (the beacon never opening). **A node that saw it and a node that did not would not agree, so this is a consensus fork.** **The oracle bit too**: seating a rejected `stake` in the same place makes `round_sequence_sound` **fire alone on an honest ledger.** The three oracle sites did carry a confirmation filter and stated it *"is never true"*, and in that wiring it became true — **the guard was not dead; only the guard was alive and the gate was dead.** The fix is **two lines**: `open_genesis` does not admit a genesis with parents. Both foldings pass through that one function so they move together, and the chosen genesis now has an empty ancestor set, which is **the base case the induction was missing**. **The determinism dump is byte-identical** — neither sweep ever built a parented genesis, which is also why the regression had to be **hand-built** (737 sweep differentials, zero mismatches). The adversary's cost was measured too: beating the minimum-tx-id rule for the genesis takes a **median of 4 rolls**, and what it buys is not new power but **the concealment of that power** (every genesis field is well-formed and the kill switch sits in one rejected parent). ⚠️ And the reason `witness_proves_default` did **not** bite through this path was **an accident of gate ordering, not the induction**. Suite 566 → **570** (four regressions).
- **Remaining abstractions**: adjudication is still a stub reflecting the measured FAR (real LLM re-execution is measured separately by B2), a VDF to remove the beacon's residual bias and P2P networking are roadmap, and A's economic model presumes four conservative simplifications.
- **Conditions for confirmation**: reproduction of the target detection rate and finalization time, the verification parameters, and the actual participation of external supply and verification nodes are confirmed by Phase 1 testnet measurement (whitepaper 10.3).
- We distinguished what the prototypes showed from what they did not, and no result is claimed beyond measurement.

- **The next slice (FEE) gave the whitepaper's twice-stated self-funding an actual prototype — and printed on the same page that the funding also flows to the attacker.** §1 and §7 both say *"the cost is **self-funded** by a 3% verification tax on new issuance"*, and the prototype had **no such mechanism at all**: `state.tax_pool` was write-only — three rules added to it, none ever subtracted. So SLH's "the slasher has no incentive" was not the cause but a **symptom**: not just the slasher, *nobody* was ever paid. One site changed (the issuance tax is split evenly among the attesting signers; only the remainder stays in the pool). ⚠️ **But no exit was created** — all three sites touching the pool are still `+=`; what changed is that the issuance tax's **inflow** shrank from `tax` to `tax % k` (corrected after pre-merge review C). **No new invariant was added** — a second site computing the same thing would inherit the same blind spot (L11), so the existing `bond_forfeiture_sound` was hardened to re-derive the signer count locally instead.
- **The self-funding floor is this slice's headline number — from what scale is the whitepaper's sentence true?** When `tax < k` every signer gets 0 and the whole tax is burned, so the **minimum issuance at which a signer receives at least 1** is, for k=1..6: **17 · 50 · 84 · 117 · 151 · 184** (boundary and boundary−1 confirmed by 12 real folds). Burn-band widths are **0 · 33 · 67 · 100 · 134 · 167**, and because the rule is `round(amount*0.03)` there was already a tax-free band at **`amount <= 16`**.
- **⚠️ And the rule handed committee capture an income — TIME's price table flipped sign.** TIME published the price of buying seed bias as *"forfeiture 0 + lock BOND×q — that money is paid by sender A, not by the adversary."* But that scenario's adversaries **sign their own mint**. Mint 50,000 → tax 1,500 → 750 to each of the two committee seats, and the bond is **not forfeited in any row**: recovery is **7.5× · 7.5× · 5.0× · 3.75× · 3.0×** for q=1..5. The price did not rise — **its sign flipped**. This is not an implementation defect but a direct consequence of *"pay the attestors"*: model self-funding without rotation or slashing and this is the number you get. **It is measured and published, not blocked.** The self-attestation axis was measured too — the effective rate matches `3%×(1−s/k)` to 1e-12, and although one account holds at most one seat (so `s` is only 0 or 1), a supplier who buys a **second address** reaches a controller rate of **0.00%**; the price of that is one keypair plus bond 100.
- **That the invariants are blind to *who* was paid is measured with a control arm, not asserted.** The same DAG is folded three ways (admission identical in all three — confirmed 8, rejected 0, supply 110,000): the real rule gives +150 to each attestor and fires **0/16**; suppressing the payout leaves 300 in the pool and fires **1/16**; **misdirecting the whole tax to the supplier fires 0/16 with the totals byte-identical.** That third arm is the honest limit this slice publishes. Of four mutations, **M4 (dropping `sorted(signers)`) is caught by nothing** — all 16 invariants stay silent **and the determinism hash does not move**, because `dump.py` already sorts balances when printing, so insertion order never reaches the published bytes. The comment beside the rule claiming *"the sort is for determinism"* was false: **what was wrong was not the code but the explanation next to it** (L16). The sort stays as a defensive measure, and the fact that no gate catches its removal is now written at that site.
- **Paying a genesis label is not payment but a burn — and the published sweep is exactly that world.** A label (`v0`-style) is not derived from any key, so no owner can pass `_check_sender`. The balance was **actually spent from**: unsigned gives `unsigned transaction`; honestly signed with the label's own attestation key gives **`sender is not the signing key's address`** (control arm: a stake-registered verifier transfers 50 of its 100 payout and it confirms). And in the determinism dump **all 74 newly appearing balance lines are labels**, and that share — **1,796** — is unspendable. ⚠️ **[Correction — pre-merge review C] "all of it is burned" does not follow, and the first edition said exactly that and was false.** A stake-registered verifier already has a balance line, so it **adds no new line** — but it is still paid: in the sortition sweep **228 existing lines received 2,440** (every one of those 228 is a 32-hex address holding 100 in the same dump's `-- stakes --`). Of the **4,236** paid out, **57.6% lands in genuinely spendable accounts**. **The label burn is a property of the fixed-committee sweep alone.** Not growing a line and not being paid are different statements.
- **Determinism moved for the first time in this slice, and the +506 bytes decompose with zero residual.** `PYTHONHASHSEED` 0, 1, 2 plus a repeat of seed 0 — all four give **1,526,834 bytes · sha256 `0cf29fe3…` · confirmed 8,240 · rejected 14,057** (the previous **1,526,328 · `2740403d…`** is kept as history). **Neither confirmed nor rejected moved by one** — the rule changes who holds value, not what the ledger admits. 506 = **74 new label balance lines, 434 bytes** + **+89 bytes** of value-width growth on **228 existing balance lines** + **−17 bytes** in the `-- tax_pool --` section (pool values shrank, so their digits did). **434 + 89 − 17 = 506, zero residual.** ⚠️ **[Correction — pre-merge review C] the first edition's decomposition attributed the wrong number** — the "+72 of value-width change" was a difflib artifact that silently netted `-- balances --` against `-- tax_pool --`, and **the "zero residual" held only because two errors cancelled.** Measured per section: balances **+523** (1,883 → 1,957 lines), tax_pool **−17**, every other section **±0**. And the very fact that **228 existing balance lines moved** independently falsifies the "all of it is burned" claim above.
- **Four things this slice did not close.** First, **`tax_pool` has no exit at all — what shrank is the issuance tax's *inflow*** (⚠️ **[Correction — review C]** the first edition said "exactly one inflow gained an exit", which is a claim about a mechanism that does not exist: all three sites touching the pool are `+=`). Whatever is already trapped, and every remainder still to accrue, has no way out. Second, **attribution to attestors is measured by scenarios, not by an invariant** (the third arm above, 0/16). Third, **bonds still cannot be withdrawn** — `unstake` does not exist, so every "price" this repo publishes is a **locking** cost, not a loss (next slice). Fourth, **the `k == 0` cell** — `_issuance_remainders` does not count the crumb of a confirmed issuance with zero valid signers; invariant 10 catches such a ledger, but until it does, invariant 16's total reads low.
- **L14 struck three times over in this slice — even counting the re-derivation sites was wrong.** The plan counted **four** places that re-derive the issuance-tax rule; the grep never looked at tests, and there were **seven**. Two of those are **mutation harnesses** that reimplement the whole `issue` branch, so leaving them stale would have smuggled a second, unintended mutation into an experiment meant to plant exactly one. The call sites of `_check_attestation` were counted as **four** and there were **five**; the missed fifth was the stub of a teeth experiment that disables the gate, so for several commits that experiment was dying with a `TypeError` instead of firing — **not green, and not measuring what it claimed to measure.** Fixing the stub then made `divmod(tax, 0)` kill the fold: the comment beside the rule saying *"there is no zero-division path"* is true **only while the gate is intact**, and this repo's teeth experiments are precisely the act of disabling that gate. The answer was a sentence the repo had already written elsewhere — **a guard is an invariant, not a type guarantee.**

## Reproducibility

Every slice has its code, tests, and results published in the repository, and reproduces deterministically (fixed seeds). That claim was itself audited — D1's random-DAG generator chose tips by iterating a `set`, so results depended on the Python hash seed (885 / 891 / 892 across processes); sorting fixed it, and we confirmed identical values at three different hash seeds.

| Slice | Code | Results |
|-------|------|---------|
| A. Issuance economics | `sim/` (60 tests) | `results/` (sweeps, frontiers, regression) |
| B2. Re-execution adjudication | `verify/` (38 fast + @slow real-model) | `verify/results/` |
| Feedback | `sim/` FAR sweep | `results/far_feedback/` |
| C. Payment channel | `channel/` (36 tests) | `results/channel/` |
| B1. PoI plumbing | `poi/` (53 tests) | `results/poi/` |
| G. Group channel | `group/` (37 tests, depends on channel for real) | `results/group/` |
| D1. DAG L1 ledger | `ledger/` (579 tests, incl. W issuance authorization, M N-party escrow, K committee, S sortition, E eligibility cutoff, P per-epoch issuance cap, A2 sender authentication, B2L beacon-on-ledger, CLK round numbering, SLH withheld-reveal forfeiture, TIME seed quorum — 550, then 555, in previous editions; slice LIV added no rule, only regressions and a scenario, and slice WALK changed not one character of the rules while folding the ancestry walk into one memoized closure and adding eleven regressions) | `results/ledger/` |
| D2. Randomness beacon | `beacon/` (24 tests) | `results/beacon/` |
| D3. PoI×adjudication wiring | `d3/` (21 tests, depends on poi for real) | `results/d3/` |
| E2E. End-to-end issuance and payment spines | `e2e/` (79 tests, depends on poi, beacon, d3, ledger, channel for real; the beacon now draws the committee **inside** the ledger) | `results/e2e/` |

A total of **927 fast tests** (914 in the previous edition, 903 before that). ⚠️ **The ledger cell of that table was two slices stale** — the **566** that summed to 914 never picked up slice FCA (+4, giving 570); adding FEE's **+9** it is re-measured here as **579** (576 passed, 3 skipped, 0 failed; 2,376 s standalone). **Collected count and passed count are not mixed** — so this total of **927 is a collected count**; what actually runs is 924 (the ledger's 3 skips). Caught by review C-m3. Real-model @slow experiments run on top of these. Each slice includes its own README and findings report, and the design and implementation plans are in `docs/superpowers/{specs,plans}/`.

---

*This document is the validation record backing the whitepaper's claims. See `docs/whitepaper/` for the whitepaper body and each directory's README for the detail of each slice.*
